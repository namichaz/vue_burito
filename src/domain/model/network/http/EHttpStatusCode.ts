enum EHttpStatusCode {
  UNKNOWN = -1,
  OK = 200,
  BAD_REQUESTV = 400,
  UNAUTHORIZED = 402,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  REQUEST_TIMEOUT = 408,
  INTERNAL_SERVER_ERROR = 500,
  SERVICE_UNAVAILABLE = 503,
  GATEWAY_TIMEOUT = 504,
}

export default EHttpStatusCode;
